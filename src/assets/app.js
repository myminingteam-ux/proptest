const menuToggle=document.querySelector('.menu-toggle');
const mobileNav=document.querySelector('.mobile-nav');
if(menuToggle&&mobileNav){menuToggle.addEventListener('click',()=>{const open=mobileNav.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open));document.body.classList.toggle('menu-open',open)});mobileNav.addEventListener('click',e=>{if(e.target.closest('a')){mobileNav.classList.remove('open');menuToggle.setAttribute('aria-expanded','false');document.body.classList.remove('menu-open')}})}

document.querySelectorAll('[data-faq]').forEach(group=>group.querySelectorAll('article>button').forEach(btn=>btn.addEventListener('click',()=>{const item=btn.closest('article');const open=item.classList.toggle('open');btn.setAttribute('aria-expanded',String(open))})));

const config=document.querySelector('[data-config]');
if(config){
  const data={
    one:{title:'1-Step Evaluation',summary:'Pass one evaluation phase under the current target and drawdown rules.',rules:[['Profit target','10%'],['Daily drawdown','3%'],['Total drawdown','6%'],['Profitable days','3'],['Time limit','No expiry']],price:'Live at checkout',note:'Evaluation pricing stays connected to the live transaction source.',cta:'View live options',href:'https://propfunded.ai/challenges'},
    two:{title:'2-Step Evaluation',summary:'Complete two 7% target phases with more drawdown room than the 1-Step structure.',rules:[['Phase targets','7% + 7%'],['Daily drawdown','5%'],['Total drawdown','10%'],['Profitable days','3 per phase'],['Time limit','No expiry']],price:'Live at checkout',note:'Evaluation pricing stays connected to the live transaction source.',cta:'View live options',href:'https://propfunded.ai/challenges'},
    instant:{title:'Instant Funding',summary:'Start at the funded-stage program structure without an evaluation phase.',rules:[['Trailing drawdown','3%'],['Total drawdown','5%'],['Max margin / pair','30%'],['Leverage','5×']],sizes:[{label:'$2.5K',price:'$199'},{label:'$5K',price:'$349'}],price:'$199',note:'Verified public fee for the $2.5K simulated balance on 2026-08-21.',cta:'View current checkout',href:'https://propfunded.ai/challenges'}
  };
  const title=config.querySelector('#programTitle'), summary=config.querySelector('#programSummary'), rules=config.querySelector('#programRules'), price=config.querySelector('#programPrice'), note=config.querySelector('#programPriceNote'), cta=config.querySelector('#programCta'), sizeSel=config.querySelector('#sizeSelector');
  function render(key){const p=data[key];title.textContent=p.title;summary.textContent=p.summary;rules.innerHTML=p.rules.map(([a,b])=>`<div><dt>${a}</dt><dd>${b}</dd></div>`).join('');price.textContent=p.price;note.textContent=p.note;cta.textContent=p.cta;cta.href=p.href;sizeSel.innerHTML='';sizeSel.classList.toggle('hidden',!p.sizes);if(p.sizes){p.sizes.forEach((s,i)=>{const b=document.createElement('button');b.type='button';b.textContent=s.label;b.className=i===0?'active':'';b.addEventListener('click',()=>{sizeSel.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');price.textContent=s.price;note.textContent=`Verified public fee for the ${s.label} simulated balance on 2026-08-21.`});sizeSel.appendChild(b)})}}
  config.querySelectorAll('.program-tab').forEach(btn=>btn.addEventListener('click',()=>{config.querySelectorAll('.program-tab').forEach(x=>{x.classList.remove('active');x.setAttribute('aria-selected','false')});btn.classList.add('active');btn.setAttribute('aria-selected','true');render(btn.dataset.program)}));render('one');
}
