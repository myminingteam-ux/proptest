import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,lastReviewed,breadcrumbs,ctaBand,migrationNote } from '../components.mjs';
import { organization,restrictedLocations } from '../site-facts.mjs';
const meta=seo({title:'About Your Funded Account | Company, Model & Transparency',description:'Learn who operates YFA, how the simulated trading evaluation model works, where the company is registered and which legal and risk disclosures govern the public program.',path:'/about/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'About',path:'/about/'}])}</div>${pageHero({eyebrow:'About YFA',title:'A trading brand should make the operating model easy to verify',lead:'The trust layer starts with legal identity and an accurate explanation of simulated trading — not fabricated scale, awards or testimonials.'})}<section class="section"><div class="wrap narrow">${directAnswer(`${organization.brandName} is operated by ${organization.legalName}, registration number ${organization.registrationNumber}, in ${organization.registeredZone}, ${organization.jurisdiction}. The public model is a crypto-focused simulated trading evaluation platform using virtual funds.`)} ${lastReviewed('Legal & Product')}<div class="entity-ledger"><div><span>Brand</span><strong>${organization.brandName}</strong></div><div><span>Legal operator</span><strong>${organization.legalName}</strong></div><div><span>Registration</span><strong>${organization.registrationNumber}</strong></div><div><span>Jurisdiction</span><strong>${organization.registeredZone}, ${organization.jurisdiction}</strong></div><div><span>Trading model</span><strong>Simulated trading evaluation</strong></div><div><span>Customer deposits</span><strong>Not accepted</strong></div></div><div class="content-flow"><h2>What simulated trading means here</h2><p>Evaluation and funded-stage accounts use virtual funds. The company does not manage customer capital and public copy must not imply direct access to deployed trading capital.</p><h2>Restricted locations</h2><p>The current terms explicitly list ${restrictedLocations.join(', ')}, in addition to sanctions and compliance restrictions.</p><h2>Team and support identity</h2><p>This candidate does not invent team members, offices, phone numbers, awards, media mentions or a support email. Those fields should appear only after identity and public-contact approval.</p>${migrationNote('Registered street address, leadership identities and public support contact details were not approved in the accessible source set. They remain production blockers for a fully completed company page, not gaps to fill with placeholders.')}</div></div></section>${ctaBand()}`;
export default {path:'/about/',seo:meta,html:layout({seo:meta,path:'/about/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'About',path:'/about/'}]) ]})};
