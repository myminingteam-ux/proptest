import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,lastReviewed,ctaBand,breadcrumbs } from '../components.mjs';
import { organization,restrictedLocations,otherEligibilityRestrictions } from '../site-facts.mjs';
const meta=seo({title:'About Your Funded Account | Company, Model & Transparency',description:'Learn who operates YFA, how the simulated trading model works, where Prop Skills Tech Ltd is registered, which locations are restricted and how to reach the current legal support contact.',path:'/about/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'About',path:'/about/'}])}</div>${pageHero({eyebrow:'About YFA',title:'A crypto evaluation brand operated by Prop Skills Tech Ltd',lead:'The legal entity and simulated trading model are the same underlying PropFunded service facts, presented under the Your Funded Account brand.'})}<section class="section"><div class="wrap narrow">${directAnswer(`${organization.brandName} is operated by ${organization.legalName}, Reg. No. ${organization.registrationNumber}, ${organization.registeredZone}, ${organization.jurisdiction}. Challenges and funded-stage accounts use simulated virtual funds.`)} ${lastReviewed('Legal & Product')}<div class="entity-ledger"><div><span>Brand</span><strong>${organization.brandName}</strong></div><div><span>Legal operator</span><strong>${organization.legalName}</strong></div><div><span>Registration</span><strong>${organization.registrationNumber}</strong></div><div><span>Jurisdiction</span><strong>${organization.registeredZone}, ${organization.jurisdiction}</strong></div><div><span>Trading model</span><strong>Simulated trading evaluation</strong></div><div><span>Legal contact</span><strong>${organization.supportEmail}</strong></div></div><div class="content-flow"><h2>What simulated trading means</h2><p>Challenges use virtual funds and do not interact with real financial markets. The company does not accept customer deposits for these trading accounts or manage customer capital through them.</p><h2>Eligibility and verification</h2><p>Explicitly restricted countries include ${restrictedLocations.join(', ')}. ${otherEligibilityRestrictions.join(', ')} are also restricted, alongside broader sanctions and compliance requirements. KYC may be requested at any stage.</p><h2>Payout review</h2><p>Payouts are conditional on rule compliance, verification and internal review. The Terms reserve the right to delay, adjust or deny payouts when suspicious or non-compliant activity is identified.</p></div></div></section>${ctaBand()}`;
export default {path:'/about/',seo:meta,html:layout({seo:meta,path:'/about/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'About',path:'/about/'}]) ]})};
