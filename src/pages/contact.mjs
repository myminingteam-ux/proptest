import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,breadcrumbs,ctaBand,migrationNote } from '../components.mjs';
import { externalFlows } from '../site-facts.mjs';
const meta=seo({title:'Contact & Support | Your Funded Account',description:'Access the current authenticated support flow while Your Funded Account public-site migration is completed. No unapproved contact details are published on this staging candidate.',path:'/contact/',noindex:true});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Contact',path:'/contact/'}])}</div>${pageHero({eyebrow:'Contact',title:'Use an approved support channel — never an invented one',lead:'The accessible source set does not include an approved public support email, phone number or service-hours claim. This staging route therefore points users to the current authenticated flow.'})}<section class="section"><div class="wrap narrow">${directAnswer('Public support contact details are not approved in this production candidate. Existing customers should use the current authenticated account/support flow until YFA support endpoints are formally migrated.')}<div class="support-panel"><h2>Current support route</h2><p>Sign in through the existing account system to access the current support path.</p><a class="btn btn-primary" href="${externalFlows.signin}">Open current sign-in</a></div>${migrationNote('Launch blocker: add only a support email, contact form, hours or phone number that Product/Support/Legal has explicitly approved for public use. This route is set to noindex until that migration is complete.')}</div></section>${ctaBand()}`;
export default {path:'/contact/',seo:meta,html:layout({seo:meta,path:'/contact/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Contact',path:'/contact/'}]) ]})};
