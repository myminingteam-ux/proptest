import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,breadcrumbs,ctaBand } from '../components.mjs';
import { externalFlows,organization } from '../site-facts.mjs';
const meta=seo({title:'Contact & Support | Your Funded Account',description:'Contact the current Prop Skills Tech Ltd support channel for YFA/PropFunded account, rule, verification and payout questions.',path:'/contact/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Contact',path:'/contact/'}])}</div>${pageHero({eyebrow:'Contact',title:'Contact the current support team',lead:'The current Terms publish contact@propfunded.ai as the legal support email for Prop Skills Tech Ltd.'})}<section class="section"><div class="wrap narrow">${directAnswer(`Email ${organization.supportEmail} for current support or legal questions. Existing customers can also sign in through the current account system.`)}<div class="support-panel"><h2>Support</h2><p><a href="mailto:${organization.supportEmail}">${organization.supportEmail}</a></p><div class="hero-actions"><a class="btn btn-primary" href="mailto:${organization.supportEmail}">Email support</a><a class="btn btn-ghost" href="${externalFlows.signin}">Open current sign-in</a></div></div></div></section>${ctaBand()}`;
export default {path:'/contact/',seo:meta,html:layout({seo:meta,path:'/contact/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Contact',path:'/contact/'}]) ]})};
