import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,lastReviewed,breadcrumbs,ctaBand } from '../components.mjs';
import { serviceFacts,platformFacts } from '../site-facts.mjs';
const meta=seo({title:'Tradable Crypto Pairs & Markets | Your Funded Account',description:'Review current PropFunded-aligned market coverage: roughly 350 USDT perpetual futures pairs, crypto and commodities, no USDC pairs, and the current CLEO fee schedule.',path:'/instruments/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Markets',path:'/instruments/'}])}</div>${pageHero({eyebrow:'Markets',title:'Crypto-focused market coverage with published platform conditions',lead:'The current public product centers on USDT perpetual futures and also states that crypto and commodities are available.'})}<section class="section"><div class="wrap narrow">${directAnswer(`The current platform description lists ${serviceFacts.markets}. USDC pairs are not supported. The dashboard remains the authoritative full instrument list.`)} ${lastReviewed('Product')}<div class="instrument-showcase"><span>BTC/USDT</span><span>ETH/USDT</span><span>SOL/USDT</span><span>XRP/USDT</span><span>Crypto + commodities</span></div><div class="content-flow"><h2>Current fee schedule</h2><p>Maker fee: <strong>${platformFacts.makerFee}</strong>. Taker fee: <strong>${platformFacts.takerFee}</strong>. Slight variations can occur because of slippage.</p><h2>Pair support</h2><p>PropFunded currently states that USDT perpetual futures pairs are supported and USDC pairs are not. The dashboard should be used for the current full list because available instruments can change.</p><h2>Leverage</h2><p>The platform default is 5× and can be adjusted, while each program remains subject to its own account and margin rules.</p></div></div></section>${ctaBand()}`;
export default {path:'/instruments/',seo:meta,html:layout({seo:meta,path:'/instruments/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Markets',path:'/instruments/'}]) ]})};
