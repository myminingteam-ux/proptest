import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,lastReviewed,breadcrumbs,ctaBand,migrationNote } from '../components.mjs';
import { serviceFacts } from '../site-facts.mjs';
const meta=seo({title:'Tradable Crypto Pairs & Markets | Your Funded Account',description:'Review the verified YFA market-coverage summary for the crypto-focused simulated trading environment, including 350+ USDT perpetual markets.',path:'/instruments/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Markets',path:'/instruments/'}])}</div>${pageHero({eyebrow:'Markets',title:'Crypto-native market coverage is the product story',lead:'YFA is positioned around USDT perpetual markets rather than presenting crypto as a secondary CFD category.'})}<section class="section"><div class="wrap narrow">${directAnswer(`The current public product is positioned around ${serviceFacts.markets}. Key examples used in the concept include BTC/USDT, ETH/USDT, SOL/USDT and XRP/USDT; the full live instrument list must remain sourced from the production market feed.`)} ${lastReviewed('Product')}<div class="instrument-showcase"><span>BTC/USDT</span><span>ETH/USDT</span><span>SOL/USDT</span><span>XRP/USDT</span><span>350+ total</span></div><div class="content-flow"><h2>Why the full list is not hardcoded</h2><p>Tradable pairs and fee schedules can change. Production YFA should render the instrument directory from the same current data source used by the trading environment, with server-side fallback data only when synchronized.</p>${migrationNote('Market-level maker/taker fees, commodities coverage and individual leverage values were not approved in the accessible source set, so they are intentionally omitted rather than invented.')}</div></div></section>${ctaBand()}`;
export default {path:'/instruments/',seo:meta,html:layout({seo:meta,path:'/instruments/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Markets',path:'/instruments/'}]) ]})};
