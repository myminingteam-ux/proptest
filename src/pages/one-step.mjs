import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,keyFacts,lastReviewed,ctaBand,breadcrumbs } from '../components.mjs';
import { programs,tradingConduct,externalFlows,evaluationRules,serviceFacts } from '../site-facts.mjs';
const p=programs.one; const meta=seo({title:'1-Step Crypto Prop Firm Challenge | Your Funded Account',description:'Review the current 1-Step simulated crypto evaluation: 10% target, 3% daily drawdown, 6% total drawdown, three qualifying profitable days, no expiry and funded-stage review.',path:'/one-step-challenge/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Programs',path:'/challenges/'},{name:'1-Step',path:'/one-step-challenge/'}])}</div>${pageHero({eyebrow:'1-Step evaluation',title:'One evaluation phase before the funded stage',lead:'A simulated evaluation with a 10% target, tighter drawdown limits and no evaluation expiry.',actions:`<div class="hero-actions"><a class="btn btn-primary" href="${externalFlows.checkout}">View live account options</a><a class="btn btn-ghost" href="/rules/">Read full rules</a></div>`})}<section class="section"><div class="wrap narrow">${directAnswer('The 1-Step evaluation has one phase with a 10% profit target, 3% daily drawdown, 6% total drawdown, three qualifying profitable days and no time limit.')} ${lastReviewed()}${keyFacts([['Profit target',p.profitTarget],['Daily drawdown',p.dailyDrawdown],['Total drawdown',p.totalDrawdown],['Profitable days',p.profitableDays],['Time limit',p.timeLimit]])}<div class="content-flow"><h2>What counts as a profitable day?</h2><p>${evaluationRules.profitableDayDefinition} At least three distinct qualifying days are required even if the target is reached earlier.</p><h2>How drawdown is measured</h2><p>The daily limit resets at ${evaluationRules.dailyReset}. Daily and total drawdown include ${evaluationRules.drawdownIncludes.toLowerCase()}.</p><h2>What happens when the target is reached?</h2><p>All positions must be closed. After successful completion, the current FAQ states the funded account is activated within ${serviceFacts.fundedActivation}. Do not trade during the review period.</p><h2>Trading conduct</h2><div class="two-column-list"><div><h3>Allowed examples</h3>${tradingConduct.allowed.slice(0,4).map(([a,b])=>`<p><strong>${a}.</strong> ${b}</p>`).join('')}</div><div><h3>Prohibited examples</h3>${tradingConduct.prohibited.slice(0,5).map(([a,b])=>`<p><strong>${a}.</strong> ${b}</p>`).join('')}</div></div><p><strong>Challenge fees are non-refundable</strong>, including after a successful evaluation.</p></div></div></section>${ctaBand('Review the 1-Step rules before checkout.','Account sizes and current fees remain owned by the live checkout flow.')}`;
export default {path:'/one-step-challenge/',seo:meta,html:layout({seo:meta,path:'/one-step-challenge/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Programs',path:'/challenges/'},{name:'1-Step',path:'/one-step-challenge/'}]) ]})};
