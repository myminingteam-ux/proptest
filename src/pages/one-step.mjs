import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,keyFacts,lastReviewed,ctaBand,breadcrumbs } from '../components.mjs';
import { programs,tradingConduct,externalFlows } from '../site-facts.mjs';
const p=programs.one; const meta=seo({title:'1-Step Crypto Prop Firm Challenge | Your Funded Account',description:'Review the YFA 1-Step simulated crypto evaluation, including the 10% target, 3% daily drawdown, 6% total drawdown, profitable-day requirement and funded-stage path.',path:'/one-step-challenge/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Programs',path:'/challenges/'},{name:'1-Step',path:'/one-step-challenge/'}])}</div>${pageHero({eyebrow:'1-Step evaluation',title:'Pass one evaluation phase and progress to the funded stage',lead:'A single-phase simulated crypto evaluation with a 10% profit target and tighter drawdown limits.',actions:`<div class="hero-actions"><a class="btn btn-primary" href="${externalFlows.checkout}">View live account options</a><a class="btn btn-ghost" href="/rules/">Read full rule summary</a></div>`})}<section class="section"><div class="wrap narrow">${directAnswer('The YFA 1-Step evaluation has one phase with a 10% profit target, 3% daily drawdown, 6% total drawdown, three profitable days and no listed evaluation expiry.')} ${lastReviewed()}${keyFacts([['Profit target',p.profitTarget],['Daily drawdown',p.dailyDrawdown],['Total drawdown',p.totalDrawdown],['Profitable days',p.profitableDays],['Time limit',p.timeLimit]])}<div class="content-flow"><h2>What happens after the evaluation?</h2><p>Meet the target and applicable risk rules, then progress through the current review and activation process to the funded stage. Payout eligibility is governed by the current funded-stage rules and Terms.</p><h2>Trading conduct</h2><div class="two-column-list"><div><h3>Allowed examples</h3>${tradingConduct.allowed.slice(0,3).map(([a,b])=>`<p><strong>${a}.</strong> ${b}</p>`).join('')}</div><div><h3>Prohibited examples</h3>${tradingConduct.prohibited.slice(0,3).map(([a,b])=>`<p><strong>${a}.</strong> ${b}</p>`).join('')}</div></div><div class="note-panel"><strong>Exact drawdown formula</strong><p>The production migration must reuse the canonical calculation logic, reset time/timezone and floating-P/L treatment from the live rule source. This candidate does not invent those details.</p><a href="${externalFlows.currentRules}">Open current canonical rules</a></div></div></div></section>${ctaBand('Review the 1-Step rules before checkout.','The public page explains the structure; the current transaction flow owns live account sizes and fees.')}`;
export default {path:'/one-step-challenge/',seo:meta,html:layout({seo:meta,path:'/one-step-challenge/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Programs',path:'/challenges/'},{name:'1-Step',path:'/one-step-challenge/'}]) ]})};
