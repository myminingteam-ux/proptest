import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,keyFacts,lastReviewed,breadcrumbs,ctaBand } from '../components.mjs';
import { serviceFacts,fundedStageFacts,platformFacts } from '../site-facts.mjs';
const meta=seo({title:'Crypto Prop Trading Platform & Simulated Environment | YFA',description:'Learn about the CLEO simulated trading environment used by PropFunded, including roughly 350 USDT perpetual pairs, iOS/Android apps, 5× default leverage, trading tools and current maker/taker fees.',path:'/platform/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Platform',path:'/platform/'}])}</div>${pageHero({eyebrow:'Platform',title:'Simulated trading through CLEO',lead:'The current PropFunded FAQ identifies CLEO as the simulated trading platform and publishes the key platform conditions below.'})}<section class="section"><div class="wrap narrow">${directAnswer(`The current program uses ${platformFacts.provider} in a simulated environment with ${platformFacts.markets.toLowerCase()}. Default platform leverage is 5× and can be adjusted; account-specific risk rules still apply.`)} ${lastReviewed('Product & Risk')}<div class="platform-source"><span>Trading platform</span><strong>${platformFacts.provider}</strong><p>${platformFacts.environment}. Evaluation and funded accounts remain demo/simulated accounts using virtual funds.</p><a href="https://cleo.finance/">View CLEO</a></div>${keyFacts([['Environment','Simulated'],['Market coverage',serviceFacts.markets],['Maker fee',platformFacts.makerFee],['Taker fee',platformFacts.takerFee],['Mobile apps',platformFacts.mobileApps.join(' & ')],['Funded leverage',fundedStageFacts.leverage]])}<div class="content-flow"><h2>Markets and fees</h2><p>Any supported USDT perpetual futures pair can be traded. USDC pairs are not supported. Maker fee is ${platformFacts.makerFee}; taker fee is ${platformFacts.takerFee}. Slight variations can occur because of slippage.</p><h2>Built-in tools</h2><p>${platformFacts.builtInTools.join(', ')} are available. External API integrations and third-party algorithmic bots are not allowed.</p><h2>Mobile and future platform support</h2><p>Mobile apps are available for iOS and Android. MT4 and MT5 support is currently described as planned for the future.</p></div></div></section>${ctaBand()}`;
export default {path:'/platform/',seo:meta,html:layout({seo:meta,path:'/platform/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Platform',path:'/platform/'}]) ]})};
