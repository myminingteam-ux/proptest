import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,lastReviewed,ctaBand,breadcrumbs,migrationNote } from '../components.mjs';
import { programs,externalFlows } from '../site-facts.mjs';
const meta=seo({title:'Crypto Prop Firm Pricing & Account Sizes | Your Funded Account',description:'Compare verified YFA program structures and currently verified instant-funded prices. Evaluation fees remain connected to the current live checkout source to avoid stale public pricing.',path:'/pricing/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Pricing',path:'/pricing/'}])}</div>${pageHero({eyebrow:'Pricing',title:'Compare account structures without stale prices',lead:'This production candidate only publishes fee values that were verified in the current source set. Evaluation pricing remains owned by the live transaction source.',actions:`<div class="hero-actions"><a class="btn btn-primary" href="${externalFlows.checkout}">Open live checkout</a><a class="btn btn-ghost" href="/challenges/">Compare programs</a></div>`})}<section class="section"><div class="wrap">${directAnswer('Instant Funding currently lists a $2.5K simulated account for $199 and a $5K simulated account for $349. 1-Step and 2-Step evaluation fees are intentionally not duplicated here until the YFA frontend is connected to the live pricing source.')} ${lastReviewed('Product')}<div class="pricing-grid"><article><span>1-Step Evaluation</span><h2>Live checkout pricing</h2><p>10% target · 3% daily drawdown · 6% total drawdown · 3 profitable days · no evaluation expiry.</p><a class="btn btn-ghost" href="${externalFlows.checkout}">View live sizes & fees</a></article><article><span>2-Step Evaluation</span><h2>Live checkout pricing</h2><p>7% + 7% targets · 5% daily drawdown · 10% total drawdown · 3 profitable days per phase · no evaluation expiry.</p><a class="btn btn-ghost" href="${externalFlows.checkout}">View live sizes & fees</a></article><article class="accent"><span>Instant Funding</span><h2>$199 / $349</h2><p>$2.5K simulated balance: $199. $5K simulated balance: $349. Funded-stage rules apply.</p><a class="btn btn-primary" href="${externalFlows.checkout}">View current offer</a></article></div>${migrationNote('Final YFA production should render all prices, account sizes, tax treatment and refund terms from the same backend pricing source used by checkout. The build should fail rather than show zero or stale values when that source is unavailable.')}</div></section>${ctaBand()}`;
export default {path:'/pricing/',seo:meta,html:layout({seo:meta,path:'/pricing/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Pricing',path:'/pricing/'}]) ]})};
