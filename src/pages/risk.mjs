import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,lastReviewed,breadcrumbs,ctaBand } from '../components.mjs';
import { organization,externalFlows } from '../site-facts.mjs';
const meta=seo({title:'Risk & Simulated Trading Disclosure | Your Funded Account',description:'Read the YFA simulated-trading disclosure and understand that evaluation and funded-stage accounts use virtual funds rather than customer deposits or managed customer capital.',path:'/risk-disclosure/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Risk disclosure',path:'/risk-disclosure/'}])}</div>${pageHero({eyebrow:'Risk disclosure',title:'Simulated trading should not be framed as deployed capital',lead:'YFA public copy states the model directly: virtual funds, defined risk rules and performance-based payout eligibility under the applicable Terms.'})}<section class="section"><div class="wrap narrow">${directAnswer(`${organization.brandName} evaluation and funded-stage accounts use simulated virtual funds. ${organization.legalName} does not accept customer deposits for trading and does not manage customer capital through these accounts.`)} ${lastReviewed('Legal & Risk')}<div class="content-flow"><h2>Trading risk still matters</h2><p>Simulated trading performance can involve significant volatility, leverage and rule-based account breaches. Results in a simulated environment do not guarantee future profitability or future payout approval.</p><h2>Performance-based payouts</h2><p>Eligibility and approval depend on the applicable program rules, review process and Terms. Public pages should not describe payouts as guaranteed.</p><h2>Current legal documents</h2><p>Until the YFA legal-document migration is complete, the current canonical Terms and Privacy Policy remain hosted on the existing PropFunded domain.</p><div class="inline-links"><a href="${externalFlows.currentTerms}">Current Terms</a><a href="${externalFlows.currentPrivacy}">Current Privacy Policy</a></div></div></div></section>${ctaBand()}`;
export default {path:'/risk-disclosure/',seo:meta,html:layout({seo:meta,path:'/risk-disclosure/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Risk disclosure',path:'/risk-disclosure/'}]) ]})};
