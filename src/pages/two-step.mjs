import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,keyFacts,lastReviewed,ctaBand,breadcrumbs } from '../components.mjs';
import { programs,tradingConduct,externalFlows,evaluationRules,serviceFacts } from '../site-facts.mjs';
const p=programs.two; const meta=seo({title:'2-Step Crypto Prop Firm Challenge | Your Funded Account',description:'Review the current 2-Step simulated crypto evaluation: two 7% targets, 5% daily drawdown, 10% total drawdown, three qualifying profitable days per phase and no expiry.',path:'/two-step-challenge/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Programs',path:'/challenges/'},{name:'2-Step',path:'/two-step-challenge/'}])}</div>${pageHero({eyebrow:'2-Step evaluation',title:'Two 7% target phases with more drawdown room',lead:'Complete both simulated phases while staying inside the 5% daily and 10% total drawdown limits.',actions:`<div class="hero-actions"><a class="btn btn-primary" href="${externalFlows.checkout}">View live account options</a><a class="btn btn-ghost" href="/rules/">Read full rules</a></div>`})}<section class="section"><div class="wrap narrow">${directAnswer('The 2-Step evaluation uses two 7% profit-target phases, 5% daily drawdown, 10% total drawdown, three qualifying profitable days per phase and no evaluation expiry.')} ${lastReviewed()}${keyFacts([['Phase targets',p.profitTarget],['Daily drawdown',p.dailyDrawdown],['Total drawdown',p.totalDrawdown],['Profitable days',p.profitableDays],['Time limit',p.timeLimit]])}<div class="content-flow"><h2>What counts as a profitable day?</h2><p>${evaluationRules.profitableDayDefinition} Each phase needs three distinct qualifying days.</p><h2>How drawdown is measured</h2><p>The daily limit resets at ${evaluationRules.dailyReset}. Daily and total drawdown include ${evaluationRules.drawdownIncludes.toLowerCase()}.</p><h2>After Phase 2</h2><p>All positions must be closed when the target is reached. After successful completion, the current FAQ states funded-account activation takes ${serviceFacts.fundedActivation}. Do not trade during the review period.</p><h2>Trading conduct</h2><div class="two-column-list"><div><h3>Allowed examples</h3>${tradingConduct.allowed.slice(0,4).map(([a,b])=>`<p><strong>${a}.</strong> ${b}</p>`).join('')}</div><div><h3>Prohibited examples</h3>${tradingConduct.prohibited.slice(0,5).map(([a,b])=>`<p><strong>${a}.</strong> ${b}</p>`).join('')}</div></div><p><strong>Challenge fees are non-refundable</strong>, including after a successful evaluation.</p></div></div></section>${ctaBand('Compare the 1-Step and 2-Step structures.','Choose based on the target and drawdown model, then verify the live checkout price.')}`;
export default {path:'/two-step-challenge/',seo:meta,html:layout({seo:meta,path:'/two-step-challenge/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Programs',path:'/challenges/'},{name:'2-Step',path:'/two-step-challenge/'}]) ]})};
