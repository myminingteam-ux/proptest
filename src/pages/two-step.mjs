import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,keyFacts,lastReviewed,ctaBand,breadcrumbs } from '../components.mjs';
import { programs,tradingConduct,externalFlows } from '../site-facts.mjs';
const p=programs.two; const meta=seo({title:'2-Step Crypto Prop Firm Challenge | Your Funded Account',description:'Explore the YFA 2-Step simulated crypto evaluation with 7% phase targets, 5% daily drawdown, 10% total drawdown, no evaluation expiry and a clear funded-stage path.',path:'/two-step-challenge/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Programs',path:'/challenges/'},{name:'2-Step',path:'/two-step-challenge/'}])}</div>${pageHero({eyebrow:'2-Step evaluation',title:'A two-phase crypto evaluation with more drawdown room',lead:'Two simulated evaluation phases, each with a 7% target, before the funded-stage review path.',actions:`<div class="hero-actions"><a class="btn btn-primary" href="${externalFlows.checkout}">View live account options</a><a class="btn btn-ghost" href="/rules/">Read full rule summary</a></div>`})}<section class="section"><div class="wrap narrow">${directAnswer('The YFA 2-Step evaluation uses two 7% profit-target phases, 5% daily drawdown, 10% total drawdown, three profitable days per phase and no listed evaluation expiry.')} ${lastReviewed()}${keyFacts([['Phase targets',p.profitTarget],['Daily drawdown',p.dailyDrawdown],['Total drawdown',p.totalDrawdown],['Profitable days',p.profitableDays],['Time limit',p.timeLimit]])}<div class="content-flow"><h2>Why choose two phases?</h2><p>The published structure offers more drawdown room than the 1-Step evaluation, in exchange for completing two target phases before the funded-stage review process.</p><h2>Trading conduct</h2><div class="two-column-list"><div><h3>Allowed examples</h3>${tradingConduct.allowed.slice(0,3).map(([a,b])=>`<p><strong>${a}.</strong> ${b}</p>`).join('')}</div><div><h3>Prohibited examples</h3>${tradingConduct.prohibited.slice(0,3).map(([a,b])=>`<p><strong>${a}.</strong> ${b}</p>`).join('')}</div></div><div class="note-panel"><strong>Exact drawdown formula</strong><p>The production migration must reuse the canonical calculation logic, reset time/timezone and floating-P/L treatment from the live rule source. This candidate does not invent those details.</p><a href="${externalFlows.currentRules}">Open current canonical rules</a></div></div></div></section>${ctaBand('Compare 1-Step and 2-Step before choosing.','Different drawdown structures can suit different trading styles.')}`;
export default {path:'/two-step-challenge/',seo:meta,html:layout({seo:meta,path:'/two-step-challenge/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Programs',path:'/challenges/'},{name:'2-Step',path:'/two-step-challenge/'}]) ]})};
