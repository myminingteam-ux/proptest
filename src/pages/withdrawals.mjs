import { seo, breadcrumbSchema } from '../seo.mjs';
import { layout,pageHero,directAnswer,lastReviewed,breadcrumbs,ctaBand,migrationNote } from '../components.mjs';
const meta=seo({title:'Payouts & Withdrawal Transparency | Your Funded Account',description:'Understand the YFA payout-review model, public-feed methodology requirements and how approved performance-based payouts are settled in USDT.',path:'/withdrawals/'});
const body=`<div class="wrap">${breadcrumbs([{name:'Home',path:'/'},{name:'Payouts',path:'/withdrawals/'}])}</div>${pageHero({eyebrow:'Payout transparency',title:'Show payout evidence only when the data is real',lead:'YFA does not manufacture payout totals, trader counts or “verified” labels. The standalone production candidate keeps a safe empty-state until a real public feed is connected.'})}<section class="section"><div class="wrap narrow">${directAnswer('Approved performance-based payouts use USDT under the current public model. This standalone YFA candidate is not connected to the live payout data source, so it does not display invented totals or misleading fallback statistics.')} ${lastReviewed('Finance, Product & Legal')}<div class="feed-state"><span>Public payout feed</span><strong>Data source not connected in this candidate</strong><p>When the production API is connected, the page should preserve the last valid snapshot during outages rather than rendering zero-value totals as a fallback.</p></div><div class="content-flow"><h2>What the production feed should show</h2><ul class="clean-list"><li>Total approved payout amount</li><li>Number of approved payouts</li><li>7-day and 30-day totals</li><li>Last successful refresh</li><li>Anonymized recent entries with status and settlement asset</li><li>Verifiable reference or transaction hash when available and safe</li></ul><h2>Methodology principles</h2><p>An approved payout and a completed payout are not the same state. The production methodology must define both, exclude duplicates, explain privacy/anonymization, document error handling and name the data owner/review date.</p>${migrationNote('Backend blocker: the public payout feed/API was not available in the accessible YFA project assets. Connecting that source is required before this route can become a full evidence page. The current safe state intentionally does not convert missing data into “0 payouts.”')}</div></div></section>${ctaBand('Prefer evidence over theatre.','YFA should only promote payout metrics that can be traced to a real, current data source.')}`;
export default {path:'/withdrawals/',seo:meta,html:layout({seo:meta,path:'/withdrawals/',body,schemas:[breadcrumbSchema([{name:'Home',path:'/'},{name:'Payouts',path:'/withdrawals/'}]) ]})};
